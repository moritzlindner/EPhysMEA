% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_General_LocationMetrics.R
\name{LocationMetrics}
\alias{LocationMetrics}
\title{Per-channel location metrics from EPhysContinuous}
\usage{
LocationMetrics(
  X,
  span = 0.3,
  parallel = !interactive(),
  progress = interactive(),
  lapply_error = c("stop", "warn")[1]
)
}
\arguments{
\item{X}{An \linkS4class{EPhysContinuous} object.}

\item{span}{Ignored (kept for backward compatibility).}

\item{parallel}{logical.
For \code{EPhysEvents} / \code{EPhysContinuous} methods only:
if \code{TRUE}, use \code{future.apply::future_lapply()} over runs.}

\item{progress}{logical.
For \code{EPhysEvents} / \code{EPhysContinuous} methods only:
show a \code{cli} progress bar in sequential mode.}
}
\value{
A \code{data.frame} with one row per channel and columns:
  \code{channel}, \code{peak_seg}, \code{peak_value}, \code{auc_norm}, \code{unit}.
}
\description{
Computes per-channel location metrics from a trial-averaged
\code{EPhysContinuous} object containing RayleighZ (or similar) values.
}
\details{
For each channel:
\itemize{
  \item \strong{peak_seg}: segment (from \code{TimeTrace}) at maximum value
  \item \strong{peak_value}: value at the peak
  \item \strong{auc_norm}: peak-normalized AUC over segments, i.e.
        the mean of \eqn{y/peak} across segments (with \code{NA} treated as 0).
}
}
