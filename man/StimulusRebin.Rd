% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_General_Bin.R
\name{StimulusRebin}
\alias{StimulusRebin}
\alias{StimulusRebin,EPhysContainer-method}
\title{Stimulus re-binning for EPhysContainer}
\usage{
StimulusRebin(X, breaks, bin_fun = mean)

\S4method{StimulusRebin}{EPhysContainer}(X, breaks, bin_fun = mean)
}
\arguments{
\item{X}{An \code{EPhysContainer}.}

\item{breaks}{Numeric vector of bin edges (strictly increasing).}

\item{bin_fun}{Function to aggregate samples within a bin (must return a single value).}
}
\value{
A numeric vector of length \code{length(breaks) - 1} with one value per bin,
  or \code{numeric(0)} if re-binning cannot be performed.
}
\description{
Re-bins the stimulus trace stored in an \code{EPhysContainer} (or subclass)
onto the bin edges provided in \code{breaks} using a per-bin aggregation
function (default: \code{mean}). Bins are treated as left-closed, right-open.
}
\details{
If either \code{TimeTrace} or \code{StimulusTrace} is missing (length 0),
or their lengths differ, this method returns \code{numeric(0)}.
}
\examples{
\dontrun{
  stim_binned <- StimulusRebin(X, breaks, mean)
}
}
